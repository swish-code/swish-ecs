export function SetupState() {
  return (
    <section className="rounded-[28px] border border-dashed border-[var(--line)] bg-[var(--surface)] p-6 text-sm leading-7 text-[var(--muted)]">
      Snowflake environment values are not configured yet. Add your connection values to the local environment file before using the admin master pages.
    </section>
  );
}
